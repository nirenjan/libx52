language: c
script: ./autogen.sh && ./configure && make V=0 && make distcheck

# Use container based infrastructure
sudo: false
dist: trusty
addons:
    apt:
        packages:
        - libusb-1.0-0-dev
        - libudev-dev
